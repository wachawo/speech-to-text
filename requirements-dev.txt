# Development tools - mirrors the `[dev]` extras in pyproject.toml. Keep in sync.
# Single-step install: `pip install -e ".[dev]"`, which is what CI uses.
# This flat file stays for environments that prefer it over an editable install.
-r requirements.txt

# Lint / format / typecheck / test runner
black>=26.5.1,<27
ruff>=0.16.7
pre-commit>=4.6.2
mypy>=2.3.1,<3
pytest>=9.1.1
pytest-cov>=7.1

# Type stubs
types-requests>=2.33.0.20260906

# Lets the tests import the real libs/diarize.py instead of only its stub.
soundfile>=0.12.1
